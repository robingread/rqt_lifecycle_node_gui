class Controller:
    def __init__(self, widget, timer) -> None:
        self._widget = None
        self._timer = None
        # Setup the signal from the Widget to the set the state of node(s).
        # Setup the timer to run at a set interval, to both discover nodes as well as update the status.

    def update(self) -> None:
        pass
